# 数签助手 - 飞书多维表格签名插件

一个为飞书多维表格设计的电子签名插件，支持手写签名并自动保存到表格记录中。

## ✨ 功能特点

- 📝 **手写签名** - 流畅的手写签名体验
- 🔗 **自动关联** - 签名自动保存到选中的表格记录
- 💾 **云端备份** - 签名文件同步到飞书云空间
- 🔐 **配额管理** - 支持免费试用和付费套餐

## 🛠️ 技术栈

| 模块 | 技术 |
|------|------|
| 前端 | Vue 3 + Vite + Element Plus |
| 后端 | Python + FastAPI |
| 数据库 | MySQL（Redis 可选） |
| 部署 | Nginx 反向代理 |

## 📁 项目结构

```
├── frontend/vue-template/  # 前端插件代码
├── backend/                # 后端 API 服务
├── nginx/                  # Nginx 配置
├── .env.example           # 环境变量模板
└── deploy-backend.sh      # 部署脚本
```

## 🚀 快速开始

### 1. 克隆项目

```bash
git clone https://github.com/your-repo/feishu-sign.git
cd feishu-sign
```

### 2. 配置环境变量

```bash
cp .env.example .env
# 编辑 .env 填写飞书 APP_ID、APP_SECRET 等配置
```

### 3. 启动后端

```bash
cd backend
pip install -r requirements.txt
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### 4. 启动前端

```bash
cd frontend/vue-template
npm install
npm run dev
```

## 📄 许可证

MIT License
